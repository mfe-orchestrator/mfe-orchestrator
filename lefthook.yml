# Lefthook configuration
# See: https://github.com/evilmartians/lefthook/blob/master/docs/configuration.md

pre-commit:
  parallel: true
  commands:
    lint-and-format:
      glob: "{backend,frontend,e2e}/**/*.{js,ts,tsx,json}"
      run: "pnpm exec biome check --write --staged {staged_files}"
      stage_fixed: true

    typecheck-backend:
      root: "backend/"
      glob: "**/*.{ts,tsx}"
      # The script checks the build and the tests, which live in two tsconfigs
      run: "pnpm run typecheck"

    typecheck-frontend:
      root: "frontend/"
      glob: "**/*.{ts,tsx}"
      run: "pnpm exec tsc --noEmit"

commit-msg:
  commands:
    commitlint:
      run: "pnpm exec commitlint --edit"
