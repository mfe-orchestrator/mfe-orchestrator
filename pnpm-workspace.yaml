packages:
  - "frontend"
  - "backend"
  - "e2e"

# Transitive dependencies pinned to patched versions.
# Both come from packages that have not yet released an updated dependency range.
overrides:
  # GHSA-w5hq-g745-h8pq — via @google-cloud/storage > gaxios
  "uuid@<11.1.1": "^11.1.1"
  # GHSA-q8mj-m7cp-5q26 — via mailinator-client > typed-rest-client (e2e only)
  "qs@>=6.11.1 <=6.15.1": "^6.15.2"
