# The images are built from the compiled frontend/backend (frontend/dist and
# backend/dist) plus a few manifests: everything else only makes the build
# context bigger and busts the cache at every local change.
**/node_modules
.git
.github
.turbo
.vscode
.windsurf

docs
e2e/test-results
e2e/playwright-report
e2e/tests
terraform

**/*.log
**/.env
upload-microfrontends
